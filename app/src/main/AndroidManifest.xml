<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.total.thecodeplace.totalapp" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="2S App"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="2S App" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AnamolyActivity"
            android:label="@string/title_activity_anamoly" >
        </activity>
        <activity
            android:name=".InitiativeActivity"
            android:label="@string/title_activity_initiative" >
        </activity>
    </application>

</manifest>
